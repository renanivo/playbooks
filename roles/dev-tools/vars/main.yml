brew_packages:
    - gnu-sed
    - fswatch
    - wget
    - ack
    - nmap
    - git-extras
    - tree

brew_cask_packages:
    - iterm2
    - xquartz
    - vagrant
    - virtualbox

pip_packages:
    - httpie

browsers:
    - google-chrome
    - firefox
