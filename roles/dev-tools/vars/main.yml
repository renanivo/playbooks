brew_packages:
    - gnu-sed
    - wget
    - ack
    - nmap
    - tree
    - boot2docker

brew_cask_packages:
    - iterm2
    - xquartz
    - vagrant
    - virtualbox

pip_packages:
    - httpie

browsers:
    - google-chrome
    - firefox
